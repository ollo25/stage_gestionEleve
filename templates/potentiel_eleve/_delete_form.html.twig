<form method="post" action="{{ path('app_potentiel_eleve_delete', {'id': potentiel_eleve.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ potentiel_eleve.id) }}">
    <button class="btn">Delete</button>
</form>
